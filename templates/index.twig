{% extends 'base.twig' %}

{% block body %}
<div class="container">
  <h1 class="mt-3 mb-5">Последние статьи в блоге</h1>
  <div class="row">
    <div class="col-9">
      {% for post in posts %}
        <div class="row mb-2 bg-light" style="border: 1px solid #dddddd;">
          <div class="p-3">
            {% if post.image_path is not null %}
              <img src="/{{ post.image_path }}" class="card-img-top" alt="{{ post.title|escape }}">
            {% endif %}
            <div class="mt-3">
              <a href="/{{ post.url_key }}"><h5 class="card-title">{{ post.title|escape }}</h5></a>
              <span class="text-muted">Олег Дерябин | {{ post.published_date|date("d F Y") }}</span>
              <p class="mt-2">{{ post.description|escape }}</p>
              <a href="/{{ post.url_key }}" class="">Читать статью</a>
            </div>
          </div>
        </div>
      {% endfor %}
      <div class="row"><a href="/blog">Смотреть все...</a></div>
    </div>
    <div class="col-3">
      <div class="container">
        <h4>Обо мне...</h4>
        <p>Привет, моё имя Олег Дерябин. Я web-разработчик и программист.</p>
      </div>
    </div>
  </div>
</div>
{% endblock %}